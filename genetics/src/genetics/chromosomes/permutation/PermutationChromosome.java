package genetics.chromosomes.permutation;

import genetics.chromosomes.AbstractChromosome;

public abstract class PermutationChromosome extends AbstractChromosome<PermutationGenotype> {

    protected PermutationChromosome(PermutationGenotype genotype) {
        super(genotype);
    }
}
